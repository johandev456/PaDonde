PORT=3000
CLIENT_URL=http://localhost:5173
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_NAME=padonde
# Optional: without a Gemini key, PaDonde still works with the built-in intent fallback.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
